using UnityEngine;
using System.Collections;

public class AnswerButton : MonoBehaviour {

    [SerializeField] private int speedH = 5;
    [SerializeField] private int speedV = 5;


	// Use this for initialization
	void Start () {
	    
	}
	
	// Update is called once per frame
	void Update () {
        //Vector3 velocity = new Vector3(speedH, speedV, 0);
        //
        //transform.position += velocity;
	}
}
